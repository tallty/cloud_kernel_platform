<%= form_for(@station_info) do |f| %>
  <% if @station_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@station_info.errors.count, "error") %> prohibited this station_info from being saved:</h2>

      <ul>
      <% @station_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
